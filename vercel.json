{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "runtime": "@vercel/node"
    }
  },
  "routes": [
    { "src": "/api/auth/(.*)", "dest": "/api/auth?route=$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" },

    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ]
}